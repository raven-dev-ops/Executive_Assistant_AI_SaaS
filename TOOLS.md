Tooling
=======

Recommended tools for working in this repo:

- Python 3.11+ (backend)
- Node.js 20+ (frontend build)
- GitHub CLI (`gh`) for issues/PRs
- Google Cloud SDK (`gcloud`) for deployments/ops (when configured)
- Docker (optional) for local container builds
- pre-commit (optional) for local lint/format/test hooks

